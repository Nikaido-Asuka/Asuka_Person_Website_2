/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['pic.imgdb.cn', 'assets.aceternity.com', 'pic1.imgdb.cn']
  }
};

export default nextConfig;
